import 'package:flutter/material.dart';

/// Navigator raíz de la app: permite navegar desde fuera del árbol de
/// widgets (p. ej. apertura desde una notificación push, Sprint 06).
final rootNavigatorKey = GlobalKey<NavigatorState>();
