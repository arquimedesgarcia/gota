# Auditoría Gota — Sprint 05 (Map)

- **Fecha:** 2026-09-12 (UTC-4)
- **Commit auditado:** `5e34092`
- **Modo:** SOLO LECTURA.
- **Alcance:** S05 — MapLibre/OSM, markers, filtros, lista/mapa. Migración 00017 (`get_map_reports`).

## Veredicto

**CONFIGURADO Y VERIFICADO.** RPC de mapa pública, privada por diseño, con límite anti-descarga y uso del índice espacial.

## Checklist

| # | Ítem | Estado | Evidencia |
|---|------|--------|-----------|
| 1 | RPC `get_map_reports` (DTO público, filtros bbox/sector/status) | OK | migración 00017; `prosrc` no contiene `created_by` |
| 2 | Grant a `anon` y `authenticated` | OK | `proacl` seteado |
| 3 | Privacidad (REQ-100): sin created_by/emails/teléfonos | OK | columnas del DTO son solo id/status/geo/conteos/nombres |
| 4 | Límite acotado 1–200 (defensa descarga) | OK | `least(greatest(...),200)` en 00017 |
| 5 | Índice espacial GIST aprovechado | OK | `reports_location_gix` existe; RPC usa `st_intersects`/`st_makeenvelope` |
| 6 | Cliente MapLibre/OSM | OK | `gota_map_view.dart` usa `maplibre_gl`; `kMapLibreDefaultStyle` vía `fromEnvironment` (default demotiles) |
| 7 | Filtros bbox capturados de MapLibre → RPC | OK | `map_filter.dart`/`map_providers.dart` pasan bbox a `get_map_reports` |

## Notas

- Estilo de MapLibre por `String.fromEnvironment` → en producción se inyecta vía `--dart-define` (no hard-codeado a demo). Pendiente operativo asignar tiles reales.
- No se detectaron discrepancias entre RPC, RLS y cliente.

## Acción necesaria

**NINGUNA** para la validación. Estilo de tiles de producción es configuración de build (fuera de alcance).
