# PROMPT 5 — HALLAZGO 5 · AJUSTES UX (TEXTO Y ORDEN DE WIDGETS)

**Clasificación:** POLISH · **Turnos:** 15-25
**Base (verificada):** `main` @ `125b0e4` — todo lo anterior **ya está commiteado** en `main`
(planes anteriores citaban `bbef6ae` + cambios sin commitear: esa base quedó obsoleta, los prompts 1 y 3
ya están en `main` y verificados). En esa base, `leak_report_screen.dart` ya
tiene `_buildContinueCallback` / `_continueWithEffectiveSelection` en `DataStepView`: **no los toques**.
**Motor:** lo decide el orquestador (no lo elijas tú).
**No** commit, **no** `git stash`, **no** `git checkout`, **no** cambiar de rama, **no** tocar otros
worktrees (`.claude/worktrees/*`).

Cinco ajustes de copy/layout pedidos por el producto. **Ningún cambio de lógica.**

## Ubicaciones verificadas en esta base

| # | Archivo:línea | Qué es |
|---|---|---|
| 5.1 | `lib/features/home/presentation/home_screen.dart:386-393` | `Text('Resumen de hoy')` + `SizedBox(height: AppSpacing.md)` |
| 5.2 | `lib/features/water/presentation/water_screen.dart:128-131` + `water_copy.dart:6` | `Text(WaterCopy.summaryTitle)` (= `'Resumen'`) + `SizedBox(height: AppSpacing.sm)` |
| 5.3 | `lib/features/leaks/presentation/leak_report_screen.dart:137-142` | leyenda `'Usa tu GPS o indica la zona manualmente. …'` + `SizedBox(height: 4)` de `:137`, bajo el título `'¿Dónde está la fuga?'` (`:133-136`) |
| 5.4 | `lib/features/leaks/presentation/leak_report_screen.dart:601-633` | tarjeta de referencia de ubicación (`if (state.locationSuggestion?.displayText != null) [...]`), hoy **antes** del dropdown de municipio (`:640-661`) y del de sector (`_SectorsDropdown`, `:676-682`) |
| 5.5 | `lib/features/leaks/presentation/leak_report_controller.dart:445-446` | `message: '¡Reporte enviado! La fuga quedó registrada como activa (ID $reportId).'` |

## Archivos que SÍ se pueden tocar

- `lib/features/home/presentation/home_screen.dart`
- `lib/features/water/presentation/water_screen.dart`
- `lib/features/water/presentation/water_copy.dart`
- `lib/features/leaks/presentation/leak_report_screen.dart` (sólo `LocationStepView` y el bloque de
  ubicación de `DataStepView`; **no** `DataStepView.build` completo ni los helpers del botón)
- `lib/features/leaks/presentation/leak_report_controller.dart` (sólo el `message` de `ReportCreated`)
- `test/features/home/community_summary_card_test.dart`, `test/features/water/water_screen_test.dart`
- `test/features/leaks/leak_report_flow_test.dart` (sólo **agregar** los tests nuevos de 5.3/5.4)

## Archivos PROHIBIDOS

`supabase/**`, cualquier otro `lib/` o test, `main`, el worktree paralelo
`.claude/worktrees/agent-a429749783d7a8538` y los demás worktrees de la tanda.

## Contrato explícito

**C1 (5.1).** Eliminar el `Text('Resumen de hoy')` y el `SizedBox(height: AppSpacing.md)` que le sigue.
La tarjeta de resumen y su contenido (`summaryAsync.when(...)`) **no** se tocan.

**C2 (5.2).** Eliminar el `Text(WaterCopy.summaryTitle)` y su `const SizedBox(height: AppSpacing.sm)`.
`_StatisticsCard` y el encabezado del historial (`WaterCopy.historyTitle`) quedan como están, sin
espaciado doble: si al quitar el bloque queda un `SizedBox` huérfano de más, ajustá **sólo** ese.
`WaterCopy.summaryTitle` (`water_copy.dart:6`) queda sin uso → **eliminá la constante** en el mismo
cambio (no dejes código muerto). Las otras constantes (`historyTitle`, `stepType`, `emptyList`) siguen
usándose: no las toques.

**C3 (5.3).** Eliminar el `const Text('Usa tu GPS o indica la zona manualmente. …')` y el
`SizedBox(height: 4)` que lo precede. El título `'¿Dónde está la fuga?'` se mantiene.

**C4 (5.4) · RESTITUIDO en la revisión v6 — SÍ ejecutar.** Decisión del dueño del producto (2026-09-23):
el punto 5.4 **se ejecuta tal como lo pide el plan**. La revisión v5 lo había descartado con el argumento
de que «el orden pedido ya es el actual»; esa afirmación es **incorrecta** contra el código verificado:
hoy el orden es `Dirección (601-633) → Municipio (643-661) → Sector (_SectorsDropdown, 676-682) →
Descripción (TextField, 684-692)`, es decir la tarjeta está **antes** del dropdown de municipio.

Mover el bloque `if (state.locationSuggestion?.displayText != null) [...Card...SizedBox(height: 8)]`
para que quede **después** del bloque `if (effectiveMunicipalityId != null) _SectorsDropdown(...)` +
su `SizedBox(height: 12)`, y antes del `TextField` de descripción. Solo reordenamiento: el contenido de
la tarjeta, sus estilos y la condición no cambian. Orden objetivo:
**«¿Dónde?» → Municipio → Sector → Dirección → Descripción**.

No aplicar ningún otro cambio a la tarjeta (ni traer las coordenadas `'Lat: …'` de `LocationStepView`
al paso de datos: eso sería un cambio nuevo, no especificado).

**C5 (5.5).** El mensaje pasa a: `'¡Reporte enviado! La fuga quedó registrada como activa.'` (sin
`(ID $reportId)`). **El `reportId` se conserva** en `outcome`/`ReportCreated`: no toques el modelo, ni
`state.copyWith`, ni la navegación. Verificá que ningún otro texto de la pantalla de resultado imprima
el id; `leak_report_screen.dart:1066` tiene el título `'Reporte enviado'` (otra cadena, **se mantiene**);
si encontrás otro texto con el id, quitalo también y decilo en el reporte.

## Criterios de aceptación

1. Home: no aparece `'Resumen de hoy'`; la tarjeta sigue renderizando sus datos.
2. Agua: no aparece `'Resumen'`; `_StatisticsCard` y el historial siguen visibles, sin espaciado doble.
3. Reportar (paso ubicación): no hay leyenda bajo `'¿Dónde está la fuga?'`.
4. Reportar (paso datos): la tarjeta de ubicación queda **después** del dropdown de sector y antes de la
   descripción.
5. Confirmación de reporte: el mensaje no contiene el id ni un UUID; el flujo y la navegación siguen
   igual.
6. `flutter analyze` sin issues nuevos (incluida la constante eliminada) y `flutter test` en verde, con
   la salida real pegada.

## Tests

**Actualizaciones (justificadas, no borrados).** Corrí el grep sobre TODO el árbol de tests: los únicos
puntos afectados son estos.

- `test/features/home/community_summary_card_test.dart:192, 206, 224, 232` — afirman
  `expect(find.text('Resumen de hoy'), findsOneWidget)`. Pasan a
  `expect(find.text('Resumen de hoy'), findsNothing)` y **se conservan** todas las aserciones de
  contenido de cada test.
- `test/features/water/water_screen_test.dart:85` — afirma `find.text(WaterCopy.summaryTitle)`. Al
  eliminar la constante pasa a `expect(find.text('Resumen'), findsNothing)`. Las demás líneas que usan
  `WaterCopy` (`:86, :106, :114`) quedan intactas.

**Tests nuevos (control de mutación obligatorio).**

- `Reportar: la leyenda bajo «¿Dónde está la fuga?» no se muestra` — en el paso de ubicación,
  `expect(find.textContaining('Usa tu GPS'), findsNothing)`.
- `Confirmación de reporte: el mensaje no expone el id` — el `message` del estado tras `ReportCreated`
  no debe contener el id del outcome.
- `Reportar (5.4): el orden del paso Datos es Municipio → Sector → Dirección → Descripción` — comparar
  las coordenadas verticales (`tester.getTopLeft(...).dy`) del dropdown de Municipio, del de Sector y de
  la tarjeta de dirección sobre los finders reales (o el orden de los hijos del `ListView`), sin
  reconstruir la pantalla a mano. **Trampa:** en el `ListView` los widgets fuera de pantalla no se
  construyen; usar `tester.scrollUntilVisible`/`ensureVisible` antes de medir.

**Regla dura de esta tanda (aprendida de la anterior):** cada test nuevo debe **fallar** si se revierte
su cambio. Está **prohibido**: reimplementar la lógica de producción dentro del test, afirmar sólo
estado del controlador cuando el criterio es visual, o dejar un test que pase en el baseline. Si un test
nuevo pasa con el cambio revertido, no sirve: borralo y escribí otro (o reportá `BLOCKER` explicando por
qué no es testeable). El orquestador va a revertir cada cambio y correr la suite para comprobarlo.

**Trampa conocida de este repositorio:** en el paso de datos, los widgets viven dentro de un `ListView`;
un `find` de algo fuera de pantalla devuelve **0 widgets** porque los items no se construyen. Usá
`tester.scrollUntilVisible(finder, 300, scrollable: find.byType(Scrollable).first)` o `ensureVisible`
antes de buscar o pulsar.

## Fuera de alcance

Cambiar copy de otros textos, rediseñar la tarjeta de resumen o de estadísticas, tocar el sistema de
diseño (`AppSpacing`/`AppColors`), el botón «Continuar» y sus helpers del prompt 3, los tests T1-T4
existentes del prompt 3, la lógica de submit, SQL, RPC, o correr `git` para revertir cambios (el control
de mutación lo ejecuta el orquestador).

## Rollback

Revertir los hunks de copy/orden y retirar los tests nuevos. La constante `WaterCopy.summaryTitle` vuelve
junto con su uso.

## Evidencia a devolver

`STATUS / CAMBIOS(archivo:línea) / ASERCIONES AJUSTADAS Y POR QUÉ / ANALYZE(salida real) /
TESTS(conteo + salida real) / ARCHIVOS / COMMIT(ninguno) / BLOCKER / NOT VERIFIED`